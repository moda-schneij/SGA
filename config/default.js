module.exports = {
  WS_PORT: 9080, //this is used by development, client-only and SER context (local)
  WEB_PORT: 9090, //this is used by development, client-only
  SER_PORT: 9080, //this is used by development, client-only and SER context (local)
  WEB_HOST: '0.0.0.0', //this is used by development, client-only
  WEB_PROTOCOL: 'http://' //this is used by development, client-only and SER context (local)
};